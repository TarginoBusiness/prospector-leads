# DASHBOARD ONLY — usado por Streamlit Cloud (deploy auto).
# Scrapers usam requirements-scrapers.txt (mais pesado, so no GH Actions).

python-dotenv==1.0.1
pyyaml==6.0.2

# Banco
psycopg[binary]==3.2.3

# Dashboard
streamlit==1.41.1
streamlit-autorefresh==1.0.1
pandas==2.2.3
plotly==5.24.1
requests==2.32.3
