# Conexao Postgres do Neon (pegue em console.neon.tech apos criar projeto)
DATABASE_URL=postgresql://user:password@ep-xxxx.region.aws.neon.tech/neondb?sslmode=require

# Modo (dev|prod) - controla logs e delays
APP_ENV=dev

# Opcional: caminho pro Tor SOCKS proxy (anti-block agressivo)
TOR_PROXY=
